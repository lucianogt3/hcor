# Próximos passos

1. Colar a versão final do Hemolabor em `backend/robots/hemolabor.py`.
2. Colar a leitura Clinux em `backend/robots/clinux.py`.
3. Ativar `ciclo_exames()` no `backend/scheduler.py`.
4. Evoluir o frontend para React/Vite depois de validar o backend.
